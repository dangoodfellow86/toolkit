import {  } from "react";

export const metadata = {
    title: "Login",
    description: "This is a Overtime Tracker",
    };
export default function LoginLayout({ children }) {

 
  return (
    <div>
      {children}
    </div>
  );
}