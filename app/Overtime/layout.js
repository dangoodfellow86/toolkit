export const metadata = {
    title: "The MIM Toolkit - Overtime page",
    description: "In this page, you can record Overtime that has been worked.",
    };
export default function OverTimeLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}