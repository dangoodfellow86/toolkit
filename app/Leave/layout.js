export const metadata = {
    title: "The MIM Toolkit - Leave page",
    description: "In this page, you can record leave requests.",
    };
export default function LeaveLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}