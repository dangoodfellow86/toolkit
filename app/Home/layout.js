export const metadata = {
    title: "The MIM tracker - Home page",
    description: "This is the dashboard page for the MIM toolkit.",
    };
export default function HomeLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}