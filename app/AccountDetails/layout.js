export const metadata = {
    title: "The MIM Toolkit - Account Details",
    description: "In this page, you can record leave requests.",
    };
export default function AccountDetailsLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}